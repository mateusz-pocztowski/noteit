query GetUserData($userId: String!) {
  user(where: { id: $userId }) {
    categories {
      id
      label
      color
      primary
      notes {
        id
        title
        content
        updatedAt
      }
    }
  }
}
