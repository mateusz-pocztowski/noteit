query GetUserData($userId: Int!) {
  user(where: { id: $userId }) {
    categories {
      id
      label
      color
      primary
      notes {
        id
        title
        updatedAt
      }
    }
  }
}
