import styled from 'styled-components'

const Wrapper = styled.div``

const NotesPage = () => {
  return (
    <Wrapper>
      <span>{/* <div>{user?.name}</div> */}</span>
    </Wrapper>
  )
}

export default NotesPage
